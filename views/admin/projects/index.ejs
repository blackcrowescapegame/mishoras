<%- include('../../partials/header') %>

<%
  function sortUrl(col) {
    const newDir = (sort === col && dir === 'asc') ? 'desc' : 'asc';
    return '?sort=' + col + '&dir=' + newDir + '&page=1' + (q ? '&q=' + encodeURIComponent(q) : '');
  }
  function sortIcon(col) {
    if (sort !== col) return '<i class="bi bi-arrow-down-up text-muted opacity-50 ms-1 small"></i>';
    return dir === 'asc'
      ? '<i class="bi bi-arrow-up ms-1 small text-primary"></i>'
      : '<i class="bi bi-arrow-down ms-1 small text-primary"></i>';
  }
%>

<div class="d-flex justify-content-between align-items-center mb-4">
  <h2 class="fw-bold mb-0">
    <i class="bi bi-kanban me-2 text-warning"></i>Proyectos
    <span class="badge bg-secondary fs-6 fw-normal ms-2"><%= total %></span>
  </h2>
  <a href="/admin/projects/new" class="btn btn-primary">
    <i class="bi bi-plus-circle me-1"></i>Nuevo proyecto
  </a>
</div>

<%- include('../../partials/alerts') %>

<div class="input-group mb-3" style="max-width:420px">
  <span class="input-group-text bg-white border-end-0"><i class="bi bi-search text-muted"></i></span>
  <input type="text" id="gridSearch" class="form-control border-start-0"
         placeholder="Buscar por nombre, cliente o ID..."
         value="<%= q %>" autocomplete="off" />
  <% if (q) { %>
  <a href="?sort=<%= sort %>&dir=<%= dir %>&page=1" class="btn btn-outline-secondary" title="Limpiar búsqueda">
    <i class="bi bi-x"></i>
  </a>
  <% } %>
</div>

<div class="card border-0 shadow-sm">
  <div class="table-responsive">
    <table class="table table-hover align-middle mb-0">
      <thead class="table-light">
        <tr>
          <th><a href="<%- sortUrl('name') %>" class="text-decoration-none text-dark">Nombre<%- sortIcon('name') %></a></th>
          <th><a href="<%- sortUrl('client_name') %>" class="text-decoration-none text-dark">Cliente<%- sortIcon('client_name') %></a></th>
          <th><a href="<%- sortUrl('custom_id') %>" class="text-decoration-none text-dark">ID Pers.<%- sortIcon('custom_id') %></a></th>
          <th class="text-end"><a href="<%- sortUrl('flat_fee') %>" class="text-decoration-none text-dark">Tarifa fija<%- sortIcon('flat_fee') %></a></th>
          <th class="text-end"><a href="<%- sortUrl('total_hours') %>" class="text-decoration-none text-dark">Horas<%- sortIcon('total_hours') %></a></th>
          <th><a href="<%- sortUrl('active') %>" class="text-decoration-none text-dark">Estado<%- sortIcon('active') %></a></th>
          <th><a href="<%- sortUrl('created_at') %>" class="text-decoration-none text-dark">Creado<%- sortIcon('created_at') %></a></th>
          <th class="text-center">Acciones</th>
        </tr>
      </thead>
      <tbody>
        <% projects.forEach(p => { %>
        <tr>
          <td><strong><%= p.name %></strong></td>
          <td><span class="badge bg-secondary-subtle text-secondary border"><%= p.client_name %></span></td>
          <td class="text-muted small"><%= p.custom_id || '—' %></td>
          <td class="text-end small"><%= p.flat_fee != null ? parseFloat(p.flat_fee).toLocaleString('es-CL', {minimumFractionDigits:2}) : '—' %></td>
          <td class="text-end small"><%= p.total_hours != null ? parseFloat(p.total_hours).toFixed(1) : '—' %></td>
          <td>
            <span class="badge <%= p.active ? 'bg-success' : 'bg-secondary' %>">
              <%= p.active ? 'Activo' : 'Inactivo' %>
            </span>
          </td>
          <td class="small text-muted"><%= new Date(p.created_at).toLocaleDateString() %></td>
          <td class="text-center text-nowrap">
            <a href="/admin/projects/<%= p.id %>/edit" class="btn btn-sm btn-outline-secondary me-1">
              <i class="bi bi-pencil"></i>
            </a>
            <form action="/admin/projects/<%= p.id %>?_method=DELETE" method="POST" class="d-inline"
                  onsubmit="return confirm('¿Desactivar este proyecto?')">
              <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
              <button type="submit" class="btn btn-sm btn-outline-danger">
                <i class="bi bi-trash"></i>
              </button>
            </form>
          </td>
        </tr>
        <% }) %>
        <% if (!projects.length) { %>
        <tr><td colspan="8" class="text-center text-muted py-4">No se encontraron proyectos.</td></tr>
        <% } %>
      </tbody>
    </table>
  </div>

  <% if (totalPages > 1) { %>
  <div class="card-footer bg-white d-flex justify-content-between align-items-center py-2">
    <small class="text-muted">
      Mostrando <%= (page - 1) * 10 + 1 %>–<%= Math.min(page * 10, total) %> de <%= total %> proyectos
    </small>
    <nav>
      <ul class="pagination pagination-sm mb-0">
        <li class="page-item <%= page <= 1 ? 'disabled' : '' %>">
          <a class="page-link" href="?page=<%= page - 1 %>&sort=<%= sort %>&dir=<%= dir %><%= q ? '&q=' + encodeURIComponent(q) : '' %>">
            <i class="bi bi-chevron-left"></i>
          </a>
        </li>
        <% for (let p = 1; p <= totalPages; p++) { %>
          <% if (totalPages <= 7 || p === 1 || p === totalPages || (p >= page - 2 && p <= page + 2)) { %>
          <li class="page-item <%= p === page ? 'active' : '' %>">
            <a class="page-link" href="?page=<%= p %>&sort=<%= sort %>&dir=<%= dir %><%= q ? '&q=' + encodeURIComponent(q) : '' %>"><%= p %></a>
          </li>
          <% } else if (p === page - 3 || p === page + 3) { %>
          <li class="page-item disabled"><span class="page-link">…</span></li>
          <% } %>
        <% } %>
        <li class="page-item <%= page >= totalPages ? 'disabled' : '' %>">
          <a class="page-link" href="?page=<%= page + 1 %>&sort=<%= sort %>&dir=<%= dir %><%= q ? '&q=' + encodeURIComponent(q) : '' %>">
            <i class="bi bi-chevron-right"></i>
          </a>
        </li>
      </ul>
    </nav>
  </div>
  <% } %>
</div>

<%- include('../../partials/footer') %>
<script>
(function(){
  const inp = document.getElementById('gridSearch');
  if (!inp) return;
  let timer;
  inp.addEventListener('input', function(){
    clearTimeout(timer);
    timer = setTimeout(function(){
      const q = inp.value.trim();
      window.location.href = '?sort=<%= sort %>&dir=<%= dir %>&page=1' + (q ? '&q=' + encodeURIComponent(q) : '');
    }, 350);
  });
  inp.focus();
  inp.setSelectionRange(inp.value.length, inp.value.length);
})();
</script>
